"use strict";

/* Package System */
const fs = require('fs');
const {check} = require('express-validator');

/* Application */

module.exports=method=>{
	let _validation = [];

	switch(method){
	}

	return _validation;
}